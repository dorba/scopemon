{
  "name": "jemal",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build-test": "tsc -p tsconfig.test.json",
    "clean-test": "rm ./test/helpers/*.js && rm ./test/*.js",
    "test": "npm run build-test && ava test --verbose && npm run clean-test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dorba/jemal.git"
  },
  "author": "Barry Dorman",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.5.2",
    "ava": "1.0.0-beta.6",
    "typescript": "^2.9.2"
  },
  "dependencies": {}
}
